import '../assets/styles/login.scss';
import LoginForm from '../components/LoginForm';

const Login = () => {
  return (
    <section id="login">
      <LoginForm />
    </section>
  );
};

export default Login;
